#include <stdio.h>
int main()
{
    int a;
    scanf("%d",&a);
    if(a>=0)
        printf("%d is a positive number",a);
    else if(a<0)
        printf("%d is a negetive number",a);
    return 0;
}

